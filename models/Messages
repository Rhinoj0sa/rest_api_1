const mongoose = require('mongoose');
const Schema = mongoose.Schema;
const MessagesSchema = new Schema({
    text: {
        type: String,
        trim: true
    },
    category: {
        type: String,
        trim: true
    },

});
module.exports = mongoose.model('Message', clientesSchema); 